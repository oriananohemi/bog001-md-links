const init = require('../index');

describe('init', () => {
  it('Debe ser una funcion', () => {
    expect(typeof init).toBe('function');
  });

  it('Debe ejecutar fileExits al cargar', () => {

    })

  it('Debe ejecutar mdlinks si recibe un archivo', () => {

  })
  });